import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snoz.dreamychristmas',
  appName: 'DreamyChristmas',
  webDir: 'dist'
};

export default config;
